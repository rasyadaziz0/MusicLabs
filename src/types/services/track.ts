export interface ParsedTrackSlug {
  cleanId: string;
  trackId: string;
}

export interface TrackSEOData {
  title: string;
  description: string;
  coverUrl: string;
  canonicalUrl: string;
  artistName: string;
  albumName: string;
}
